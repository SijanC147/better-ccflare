# better-ccflare

Load balancer proxy for Claude Code. Spreads requests across many account providers → optimal rate limit management.

## CRITICAL: Account & File Safety

**Testing Endpoint**: Automated/scripted testing → use non-Anthropic accounts (ollama, litellm, omniroute, etc.). Real Anthropic accounts get banned for automated usage. The `claude` account is reserved for real Claude Code usage only. Force-route testing via `x-better-ccflare-account-id` header.

**NEVER contact upstream.** No pushes, PRs, issues, comments, bug reports, or any other outbound communication to `tombii/better-ccflare` — or any upstream/third-party repo — unless Sean explicitly says so in that session, and then only after confirming twice. This holds even when we find a genuine upstream bug worth reporting: record it locally (MERGELOG, Linear) and stop. Upstream remotes stay fetch-only (`no_push`). Finding a real defect in upstream code is **not** authorisation to tell them about it.

**Generated files** — tracked, but regenerated by `apps/cli/prepare-workers.ts` during any build. Never hand-edit; review as build output:
- `packages/database/src/inline-vacuum-worker.ts`
- `packages/database/src/inline-incremental-vacuum-worker.ts`
- `packages/database/src/inline-integrity-check-worker.ts`

If these change when you did not touch worker source, that is a signal (usually wrong Bun version), not churn to commit away. `packages/dashboard-web/dist/**` is also generated, but gitignored.

`packages/proxy/src/inline-worker.ts` is tracked but **orphaned** — nothing imports it since the UsageCollector migration, and no build regenerates it. It is not a live protected file.

**Other**: modify only `./README.md` (root). Keep `apps/cli/README.md` untouched.

## Quick Start

**Build & run**: `bun run build && bun start` (port 8080)

**Quality gate** (run before opening a PR — mirrors CI):
```bash
bun install --frozen-lockfile
bun run typecheck
bun test                              # includes scripts/hextap-contract.test.ts
bun run build:dashboard
bun run scripts/test-hextap-build.ts
brew hextap validate --project .
```
`bun run lint` (`biome check --write --unsafe`) and `bun run format` **modify files**. Useful, but not a pass/fail gate. Authoritative gate is `.github/workflows/ci.yml`.

**Git safety**:
- `git status` before any changes — track pre-existing uncommitted files
- `main` is **protected**: PR-only, two required checks, zero bypass actors (ruleset `hextap/main`, ID `21627513`). Never push to it, never force-push, never seek admin bypass
- Branch from main: `git switch main && git pull --ff-only origin main && git switch -c feat/name`
- Push the feature branch: `git push -u origin feat/name`
- Repo has both a `main` branch and a `main` tag. **Always use `refs/heads/main`** (not bare `main`) for git log/diff/checkout/merge-base → avoid ambiguous-refspec errors
- Fetch upstream without tags (`git fetch upstream --prune`) — fork and upstream tag names collide
- Commit: `git add <specific-files>` only

**Version** — the **git tag is the version**. Hextap injects it plus the tagged commit as compile-time defines; a release binary reports `better-ccflare 3.8.2 (commit <sha>)`. Hextap does **not** edit `package.json` or `apps/cli/package.json` — they lag the release on purpose. `packages/core/src/version.ts` is not a version-bump target (it holds `CLAUDE_CLI_VERSION`). See `docs/release.md`.

`v*` tags are immutable (ruleset `hextap/release-tags`, ID `21627514`). Fix a failed release forward with a new version, never by re-tagging.

## ⚠️ Database Migrations — Port to PostgreSQL

Every migration added to `packages/database/src/migrations.ts` MUST also be ported to `packages/database/src/migrations-pg.ts`:
1. `ensureSchema()` in `migrations.ts` (SQLite CREATE TABLE)
2. `runMigrations()` in `migrations.ts` (SQLite ALTER TABLE for existing DBs)
3. `ensureSchemaPg()` in `migrations-pg.ts` (PG CREATE TABLE for new installs)
4. `columnsToAdd` array in `runMigrationsPg()` (PG ALTER TABLE for existing DBs)
5. Mirror any SQLite backfill as an `adapter.unsafe(UPDATE ...)` in `runMigrationsPg()`

New tables go in `ensureSchemaPg()` AND `runMigrationsPg()` (`CREATE TABLE IF NOT EXISTS`).

## Development Workflow

**New functionality** — Write tests first, then implement, then run tests.

**Multi-task sessions & plans** — Spawn subagents for independent work (code, research, testing, exploration); fresh subagent per task. Sequential execution wastes context.

**External-contributor PRs** — Merge with `git merge --no-ff <branch>` (preserve contributor history), then add to README Acknowledgements.

**Issue management** — Never close issues automatically; wait for reporter confirmation. Before implementing an issue, run `git log refs/heads/main --since='<issue-open-date>' --oneline --no-merges -- <paths>` and confirm it still applies given recent changes (rate-limit/health/proxy code changes often).

## Commands

**Server**:
- First run: `bun run build`
- Dev: `bun start --serve --port 8081` (test on 8081, not production 8082)
- Startup ~15s; wait before testing

**Account management**:
```bash
bun run cli --add-account <name> --mode <mode> --priority <n>
bun run cli --list
bun run cli --remove <name>
bun run cli --reauthenticate <name>
bun run cli --set-priority <name> <priority>
bun run cli --reset-stats
```

**Testing OpenRouter**: Model `z-ai/glm-4.5-air:free`
```bash
curl -X POST http://localhost:8081/v1/messages \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer test" \
  -d '{"model":"z-ai/glm-4.5-air:free","messages":[{"role":"user","content":"test"}],"max_tokens":10}'
```

## Releases (Hextap)

Releases run on [Hextap](https://github.com/SijanC147/hextap-toolkit). `.github/workflows/release.yml` is gone; `.github/workflows/hextap-release.yml` is a thin caller pinned to toolkit `v0.4.2` / `613f0d37a0c84cff20a8e277fc5e9c374f9cbc26`. Never float that pin.

A release is a pushed `v*` tag on protected main — nothing else starts one. It does not have to be annotated: the workflow triggers on `push: tags: v*`, `v3.8.1` shipped on a lightweight tag, and a tag created by publishing a GitHub release is lightweight. See `docs/release.md`. Prereleases (`v3.8.3-rc.1`) stop after GitHub publication; stable tags also update the private Homebrew tap.

`.hextap.json`, `.hextap/tap-registration.json`, and the tap's `Projects/better-ccflare.json` must stay **byte-identical**. No field is safe to edit unilaterally — it is a release and recovery boundary, and reformatting counts as a change.

**Read `docs/release.md` before touching anything under `.hextap/`, `scripts/hextap-*`, `.github/workflows/`, or release tags.** The installed `hextap` skill covers toolkit operation; `/ask-hextap-coordinator` has the migration's decision history.

## More Details

- **Releases & publishing**: `docs/release.md` (tracked)
- **Rollback & recovery**: `docs/rollback.md` (tracked) — `hextap rollback` is package-only; the DB is not rolled back and `#340` dedup deletions are unrecoverable without a backup taken first
- **Upstream sync calibration**: `docs/upstream-sync.md` (tracked)
- **CLI & account setup**: `.claude/docs/cli-commands.md` *(gitignored — absent in fresh clones)*
- **Database configuration**: `.claude/docs/database.md` *(gitignored)*
- **GitNexus code intelligence**: `.claude/docs/gitnexus.md` *(gitignored)*

## Linear (canonical task tracking)

- `linear_project`: better-ccflare — id `4317856f-1f0f-449a-b311-c211d5fc6528`, team `SB23`
- URL: https://linear.app/sb23/project/better-ccflare-9139ce4ab6ff
- `linear_last_sync`: 2026-08-28 @ 7d95892b
- Hextap migration tracked as `SB23-642` (Done) — canonical audit trail for why the release architecture looks the way it does

## Next Actions

- **v3.12.0 is released and fully published, tag at `048b8631`.** RC-first (`v3.12.0-rc.1` on the same commit), 8 assets, 5 targets, `Publish Homebrew Formula` succeeded — the tap gate that blocked v3.10.0 is fixed. It carries the OpenObserve work end to end: PR #63 the exporter, #64 response-header sanitization, #65 dashboard configuration, #66 the manifests. Sean was still running 3.10.0 locally as of the release; `brew upgrade better-ccflare && brew services restart better-ccflare` needs his explicit go every time
- **Shipping to OpenObserve works and was proven live, not assumed.** App logs come off `logBus`; request and response payloads are shipped without ever reaching the database when `store_payloads` is off. Off by default — a configured base URL is the switch, and `openobserve_ship_payloads` separately decides whether records carry bodies. Configure it from the dashboard (`GET`/`POST /api/config/openobserve`) or by environment variable. **Read `mem:openobserve_telemetry_seams` before touching any of it**: it records the two gates that both had to widen, the `logBus` move to `packages/logger/src/log-bus.ts`, and four OpenObserve behaviours that mislead — in particular that a stream's `doc_num` lags so badly it reports a working exporter as dead, so verify with `POST /api/{org}/_search` and never with stream counts. Open follow-ups: `SB23-1760` (retry, log min-level filter, per-account attributes, metrics, traces, OTLP, disk queue), `SB23-1769` (`bun run lint` rewrites 31 unrelated files including all three generated workers)
- **`.mcp.json` is gitignored here as of `048b8631`, and that is deliberate.** The format stores credentials as literal header and env values rather than `${VAR}` expansions. Measured across seven repos on this machine: every repo that tracks one has live keys committed, and `SijanC147/claude-peers-mcp` is **public** with five of them in `origin/main` at `df23ada`. Never track this file.
- **v3.10.0's Homebrew publication is resolved and no longer outstanding.** It failed on the tap's own `Published Formula gate`, not on anything in this repo: the gate lints the whole tap and `Formula/headphones.rb:325` used `def post_install`, which Homebrew now rejects. HTAP-000 fixed it (tap `main` green, three blockers not one), and every release since has published cleanly. Kept here because the diagnosis is reusable: a `Publish Homebrew Formula` failure is usually the tap, not the release, and `Formula/better-ccflare.rb` carrying the right URLs and sha256s is the evidence that the release itself completed. Recovery for a homebrew-only republish is `gh workflow run hextap-release.yml --repo SijanC147/better-ccflare --ref main -f tag=<tag>`. Three fix routes that do NOT work, measured: inline `rubocop:disable` is banned by `Style/DisableCopsWithinSourceCodeDirective`; a tap `.rubocop.yml` is ignored because `brew style` forces `--config`; the steps-DSL migration clears the offence then trips on `#{HOMEBREW_PREFIX}` inside `args:`
- **The config file is 0600 as of PR #57 (`f2dc5c8e`), tracked as `SB23-1709`.** It held `pg_password`, `local_control_secret` and the maintainer PAT at 0644. Fourteen review rounds found six further reachable defects, each one introduced by the previous round's fix: an authentication bypass through an ungated read, an arbitrary file overwrite through unchecked intermediate symlink hops, a config path naming a directory losing its execute bits (which locks the operator out of `better-ccflare.db` in the same directory), ownership discarded by the rename, dangling and multi-hop links destroyed, and `/etc` plus bind-mount layouts silently ceasing to persist. Three mutation rows survive and are disclosed in the commit rather than buried: ownership preservation, the Windows gate, and fsync, each needing root, a second account, or a Windows host. Open follow-ups: `SB23-1700` (regular-file config injection plus the FIFO that hangs startup forever), `SB23-1696`, `SB23-1686`, `SB23-1679` (the 0644 `better-ccflare.db` with plaintext tokens beside the config — a bigger exposure than the file #57 fixed), `SB23-1680`
- **The upstream maintainer is live and has completed a real sync, tracked as `SB23-1645`.** PR #52 merged the first App-produced sync: 106 commits, 121 files, `4d27cb22..ea0e3320`. The fork is level with upstream. All 15 gates passed first time with zero repair rounds and the reviewer approved on specifics. Read `mem:upstream_maintainer_gates` before editing `.github/upstream-maintainer.yml`: `protected_paths` alone protects nothing, `forbidden_patterns` reads only the upstream tree, and `*` never crosses a slash in a glob
- **Three triggers, all proven: the Monday schedule, manual dispatch, and a webhook.** The webhook is `repository_dispatch` with `event_type: sync-upstream` and a `client_payload.target` filtered against `TARGETS`. newreleases.io can POST GitHub's dispatch endpoint directly, so no relay is needed; upstream publishes ~9 releases a week, which is a sharper signal than the cron
- **PR #55 turned on version proposals and draft releases, and neither has ever run.** A merged sync PR now drafts a release. **Publishing that draft is irreversible**: it creates the tag at the pinned commit, fires `hextap-release.yml`, and `v*` tags cannot be moved or deleted. The order inverts from the old hand flow — draft first, THEN the manifest-bump PR, then retarget the draft, then publish. `version.kind` must stay `fork-semver`: Hextap rejects any tag containing `+` at source, so the upstream-derived scheme would fail the release on an undeletable tag
- Still unexercised anywhere: the repair path, feedback rounds (`FEEDBACK_ENABLED` unset), and the whole draft-release path. The first sync after upstream publishes is the test
- Three controller defects were found only by running it, all fixed in `SijanC147/upstream-maintainer`: the target branch was fetched by bare name and this repo has both a `main` branch and a `main` tag, so every run aborted in `prepare` (#34); agent stdout was capped at 1 MiB, which a 106-commit review exceeded (#35); and `agent.max_repair_rounds` plus `agent.timeouts.repair_minutes` were parsed and then consumed by nothing, so every agent session was killed at 8 minutes regardless of config (#36). Treat a config key as inert until you have found its consumer in the controller's source
- CI skips its expensive steps when nothing that can change a result changed (PR #49). `scripts/ci-changed-paths` decides, the trigger is deliberately NOT path-filtered because both job names are required checks and a skipped workflow never reports them, and steps are gated on `!= 'false'` so a missing output still runs everything. Measured: a `.github/**` comment-only PR finishes `Bun and release tooling` in 8 seconds, green
- Backlog: `SB23-313` (28 pre-existing biome lint errors), `SB23-314` (non-deterministic `bun test` exit code — downgraded to Low 2026-08-28; the original evidence compared different checkouts rather than repeats in one tree, and the exit code is now consistently 1, correctly reflecting the two macOS-only failures) — both non-blocking tech debt
- Two tests fail on macOS only and pass on Linux CI: `AgentRegistry — injected workspace persistence` (both cases). Cause is macOS `/var` → `/private/var` realpath, not a defect. Do not "fix" them by loosening the assertion
- Upstream sync: last completed PR #52 (2026-09-13, 106 commits `4d27cb22..ea0e3320`), the first produced by the App rather than by hand. Next anchor: `last-sync-sha ea0e3320` in `MERGELOG.md`, which the maintainer now advances itself. Syncs are the App's job now; `/sync-upstream` is the manual fallback. PR #35 (raw `tombii:main → main` pull) was closed unmerged — it would have dropped the Hextap files; never merge that bot PR
- `/sync-upstream` is a **user-scoped, repo-agnostic** command; this repo's specifics live in tracked `docs/upstream-sync.md` (remotes, ref-ambiguity, protection, commands, generated files, inviolable Hextap paths, standing conflict resolutions, and the "breaks without conflicting" audit). Read that before any sync
- `brew hextap validate --build` refuses unless the local Bun is **exactly 1.3.14** (CI pin). A newer local Bun can still run every other gate step — the five-target compile is proven by cutting an `-rc.N` tag instead

## Commit Messages

Conventional-commit prefixes (`feat:`, `fix:`, `security:`, `refactor:`, …). Convention only — nothing parses them. The old prefix-driven release automation was removed with `release.yml`; the git tag alone determines a release.