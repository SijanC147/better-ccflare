# better-ccflare

Load balancer proxy for Claude Code. Spreads requests across many account providers → optimal rate limit management.

## CRITICAL: Account & File Safety

**Testing Endpoint**: Automated/scripted testing → use non-Anthropic accounts (ollama, litellm, omniroute, etc.). Real Anthropic accounts get banned for automated usage. The `claude` account is reserved for real Claude Code usage only. Force-route testing via `x-better-ccflare-account-id` header.

**NEVER contact upstream.** No pushes, PRs, issues, comments, bug reports, or any other outbound communication to `tombii/better-ccflare` — or any upstream/third-party repo — unless Sean explicitly says so in that session, and then only after confirming twice. This holds even when we find a genuine upstream bug worth reporting: record it locally (MERGELOG, Linear) and stop. Upstream remotes stay fetch-only (`no_push`). Finding a real defect in upstream code is **not** authorisation to tell them about it.

**Generated files** — tracked, but regenerated by `apps/cli/prepare-workers.ts` during any build. Never hand-edit; review as build output:
- `packages/database/src/inline-vacuum-worker.ts`
- `packages/database/src/inline-incremental-vacuum-worker.ts`
- `packages/database/src/inline-integrity-check-worker.ts`

If these change when you did not touch worker source, that is a signal (usually wrong Bun version), not churn to commit away. `packages/dashboard-web/dist/**` is also generated, but gitignored.

`packages/proxy/src/inline-worker.ts` is tracked but **orphaned** — nothing imports it since the UsageCollector migration, and no build regenerates it. It is not a live protected file.

**Other**: modify only `./README.md` (root). Keep `apps/cli/README.md` untouched.

## Quick Start

**Build & run**: `bun run build && bun start` (port 8080)

**Quality gate** (run before opening a PR — mirrors CI):
```bash
bun install --frozen-lockfile
bun run typecheck
bun test                              # includes scripts/hextap-contract.test.ts
bun run build:dashboard
bun run scripts/test-hextap-build.ts
brew hextap validate --project .
```
`bun run lint` (`biome check --write --unsafe`) and `bun run format` **modify files**. Useful, but not a pass/fail gate. Authoritative gate is `.github/workflows/ci.yml`.

**Git safety**:
- `git status` before any changes — track pre-existing uncommitted files
- `main` is **protected**: PR-only, two required checks, zero bypass actors (ruleset `hextap/main`, ID `21627513`). Never push to it, never force-push, never seek admin bypass
- Branch from main: `git switch main && git pull --ff-only origin main && git switch -c feat/name`
- Push the feature branch: `git push -u origin feat/name`
- Repo has both a `main` branch and a `main` tag. **Always use `refs/heads/main`** (not bare `main`) for git log/diff/checkout/merge-base → avoid ambiguous-refspec errors
- Fetch upstream without tags (`git fetch upstream --prune`) — fork and upstream tag names collide
- Commit: `git add <specific-files>` only

**Version** — the **git tag is the version**. Hextap injects it plus the tagged commit as compile-time defines; a release binary reports `better-ccflare 3.8.2 (commit <sha>)`. Hextap does **not** edit `package.json` or `apps/cli/package.json` — they lag the release on purpose. `packages/core/src/version.ts` is not a version-bump target (it holds `CLAUDE_CLI_VERSION`). See `docs/release.md`.

`v*` tags are immutable (ruleset `hextap/release-tags`, ID `21627514`). Fix a failed release forward with a new version, never by re-tagging.

## ⚠️ Database Migrations — Port to PostgreSQL

Every migration added to `packages/database/src/migrations.ts` MUST also be ported to `packages/database/src/migrations-pg.ts`:
1. `ensureSchema()` in `migrations.ts` (SQLite CREATE TABLE)
2. `runMigrations()` in `migrations.ts` (SQLite ALTER TABLE for existing DBs)
3. `ensureSchemaPg()` in `migrations-pg.ts` (PG CREATE TABLE for new installs)
4. `columnsToAdd` array in `runMigrationsPg()` (PG ALTER TABLE for existing DBs)
5. Mirror any SQLite backfill as an `adapter.unsafe(UPDATE ...)` in `runMigrationsPg()`

**An `accounts` column needs two more places, for seven in total, and nothing points at them:**
6. The `INSERT INTO accounts_new SELECT` column list at `migrations.ts:1212-1226`
7. The second rebuild's column list at `migrations.ts:1580-1592`

Both are explicit column lists in table rebuilds, so a column missing from one is **dropped
silently** when a database takes that branch, with no constraint violation and no log line.
Measured 2026-09-15: both lists already omit `consecutive_rate_limits` and
`last_manual_reauth_at`, which is `SB23-2073`. Check whether your column belongs in a rebuild
before assuming five places is the whole rule.

New tables go in `ensureSchemaPg()` AND `runMigrationsPg()` (`CREATE TABLE IF NOT EXISTS`).

## Development Workflow

**New functionality** — Write tests first, then implement, then run tests.

**Multi-task sessions & plans** — Spawn subagents for independent work (code, research, testing, exploration); fresh subagent per task. Sequential execution wastes context.

**External-contributor PRs** — Merge with `git merge --no-ff <branch>` (preserve contributor history), then add to README Acknowledgements.

**Issue management** — Never close issues automatically; wait for reporter confirmation. Before implementing an issue, run `git log refs/heads/main --since='<issue-open-date>' --oneline --no-merges -- <paths>` and confirm it still applies given recent changes (rate-limit/health/proxy code changes often).

## Commands

**Server**:
- First run: `bun run build`
- Dev: `bun start --serve --port 8081` (test on 8081). **The Homebrew service listens on 8080**, verified 2026-09-13 with `lsof -nP -iTCP -sTCP:LISTEN`. This file said 8082 for months and that was wrong in the dangerous direction: an agent told 8082 is production reads 8080 as free and binds a test server on top of the live one
- Startup ~15s; wait before testing

**Account management**:
```bash
bun run cli --add-account <name> --mode <mode> --priority <n>
bun run cli --list
bun run cli --remove <name>
bun run cli --reauthenticate <name>
bun run cli --set-priority <name> <priority>
bun run cli --reset-stats
```

**Testing OpenRouter**: Model `z-ai/glm-4.5-air:free`
```bash
curl -X POST http://localhost:8081/v1/messages \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer test" \
  -d '{"model":"z-ai/glm-4.5-air:free","messages":[{"role":"user","content":"test"}],"max_tokens":10}'
```

## Releases (Hextap)

Releases run on [Hextap](https://github.com/SijanC147/hextap-toolkit). `.github/workflows/release.yml` is gone; `.github/workflows/hextap-release.yml` is a thin caller pinned to toolkit `v0.4.2` / `613f0d37a0c84cff20a8e277fc5e9c374f9cbc26`. Never float that pin.

A release is a pushed `v*` tag on protected main — nothing else starts one. It does not have to be annotated: the workflow triggers on `push: tags: v*`, `v3.8.1` shipped on a lightweight tag, and a tag created by publishing a GitHub release is lightweight. See `docs/release.md`. Prereleases (`v3.8.3-rc.1`) stop after GitHub publication; stable tags also update the private Homebrew tap.

`.hextap.json`, `.hextap/tap-registration.json`, and the tap's `Projects/better-ccflare.json` must stay **byte-identical**. No field is safe to edit unilaterally — it is a release and recovery boundary, and reformatting counts as a change.

**Read `docs/release.md` before touching anything under `.hextap/`, `scripts/hextap-*`, `.github/workflows/`, or release tags.** The installed `hextap` skill covers toolkit operation; `/ask-hextap-coordinator` has the migration's decision history.

## More Details

- **Releases & publishing**: `docs/release.md` (tracked)
- **Rollback & recovery**: `docs/rollback.md` (tracked) — `hextap rollback` is package-only; the DB is not rolled back and `#340` dedup deletions are unrecoverable without a backup taken first
- **Upstream sync calibration**: `docs/upstream-sync.md` (tracked)
- **CLI & account setup**: `.claude/docs/cli-commands.md` *(gitignored — absent in fresh clones)*
- **Database configuration**: `.claude/docs/database.md` *(gitignored)*
- **GitNexus code intelligence**: `.claude/docs/gitnexus.md` *(gitignored)*

## Linear (canonical task tracking)

- `linear_project`: better-ccflare — id `4317856f-1f0f-449a-b311-c211d5fc6528`, team `SB23`
- URL: https://linear.app/sb23/project/better-ccflare-9139ce4ab6ff
- `linear_last_sync`: 2026-08-28 @ 7d95892b
- Hextap migration tracked as `SB23-642` (Done) — canonical audit trail for why the release architecture looks the way it does

## Next Actions

- **The releases so far**: v3.11.0, v3.12.0 (OpenObserve), v3.13.0 (`/v1/models` for OpenAI clients), v3.14.0 (API playground, version-widget recheck, the 8080 correction), v3.15.0 (config and database file modes, the non-regular-file config guard, `upstream_error` alerts, `PATCH /api/accounts/:accountId`, the kiosk view), v3.16.0 (`#88` peak hours in the viewer's timezone with a countdown, `#91` permission-guard tests), v3.17.0. Every one RC-first on the same commit, 8 assets, 5 targets, Homebrew published and the formula confirmed pointing at the new tarballs each time. **Sean was still running 3.10.0 locally**, so his install predates all seven; `brew update && brew upgrade better-ccflare && brew services restart better-ccflare` needs his explicit go every time and has never been run for these
- **v3.17.0 carried four PRs**: `#93` the Anthropic peak-hour window defined in the vendor's own timezone, `#95` Claude service status surfaced from `status.claude.com`, `#96` circuit-breaker state reported on `/api/health` without gating, `#94` the OpenObserve token fields pinned
- **v3.19.0 is the current release, tag at `6a0e0cc7`, fully published.** It carries `#106` `GET /api/meta/routes` and `#107` per-slot throttle thresholds on combo members. **Nine releases have gone out across 2026-09-13 and 14**, every one RC-first on the same commit, 8 assets, 5 targets, Homebrew published and the tap formula confirmed each time. v3.18.0 (`374f6f8f`) carried `#100`, `#103`, `#101`, `#102` and `#98`
- **v3.21.0 is the current release, tag at `ce05afce`, 8 assets across 5 targets, Homebrew formula confirmed pointing at the v3.21.0 tarballs.** It carries `#116` the forward-slug project fix, `#118` `cwdHint` at the third `forwardToClient` site, `#119` the `PROJECTS_CASE_SENSITIVE` boot guard, `#120` the frozen-clock test fix, `#121` the retry settings card, plus `#117` and `#122` docs. v3.20.0 (`06107786`) carried `#110`, `#111`, `#112`, `#113`. Every release this week went RC-first on the same commit. **`main` is past the tag again**: `#123` manifests, `#124`/`#125` the retry clamp, `#128` the session-duration report, `#129` the page-size warning, `#130` the resolved jitter ceiling, `#131` the per-slot throttle UI, `#133` the grid-view overflow fix, `#132` docs
- **v3.22.0 is the current release, tag at `b9852647`, 8 assets across 5 targets, Homebrew formula confirmed at `eab7ae3`.** RC-first on the same commit as always. **The host is finally on it**: Sean upgraded from 3.10.0 and restarted, pid 4228 on 8080 resolves to `Cellar/better-ccflare/3.22.0/bin/better-ccflare`, no binary-versus-Cellar skew, and `SB23-750` is closed after fourteen releases of waiting. `main` is past the tag again: `#135` the formatting gate, `#136` docs, `#137` the `min_reset_remaining_ms` bound, `#138` the migration rebuild columns, `#139` the catalog PATCH entry, `#140` the backup file modes
- **Formatting is enforced now, and turning it on found five errors this file claimed did not exist.** `#135` added a read-only `bunx biome check .` step inside `Bun and release tooling`. The tree measured **three errors** before it, and clearing them exposed **two more**, all five in files written for the `SB23-1975` forward-slug work. **Run `bunx biome check --write <your files>`, scoped, before opening a PR. Never `bun run lint`**: it is `--write --unsafe`, mutates the tree, and can exit 0 having rewritten code, so it cannot tell you whether CI will pass
- **OpenObserve is live on this host as of 2026-09-17.** Shipping to `https://o2.seanbugeja.me`, org `default`, as `better-ccflare-ingest@seanbugeja.me`, streams `better_ccflare_logs` and `better_ccflare_requests`, INFO, payloads off. Credential in 1Password vault `Agents`, item **better-ccflare OpenObserve ingestion**. **Verify with `_search`, never with stream counts** — `doc_num` lags badly enough to report a working exporter as dead, and an ingest-scoped token gets 401 on `_search` so that check needs the root credential. Cloudflare fronts the host but does not gate `/api/`. Before creating another service account read `mem:openobserve-service-account-tokens`: the list endpoint returns a **masked 12-character token for every account**, including working ones, and the real 16-character token comes only from creation or `?rotateToken=true`
- **Attribution has no input, and this is not a better-ccflare bug.** Measured 2026-09-17: 40,734 requests, **164** with a `project_id`, all 164 still joining to `projects`, and of 41,072 requests since 09-15 exactly **one** carried a `worktree_path`. The decisive number is that **zero** requests have a `worktree_path` and a null `project_id` — so nothing that had the input failed to resolve. The resolver `#116` fixed is correct; what does not exist is the sending side. `SB23-2236`, related to `SB23-1296`, and the work is host-level in `claude-meta`. **Do not go looking for a resolver regression here**. **Corrected 2026-09-18 by the `claude-meta` lane, twice over.** The sender is not missing, it is *inert*: `claude-meta/tools/ccflare-project-headers.py` (PR #65, `745b555`) writes `ANTHROPIC_CUSTOM_HEADERS` with `X-CCFlare-CWD` and produced the first attributed rows; nothing ran it. "Not running" and "does not exist" give the same measurement and need different work. And **20 of the 21 files under `~/Code` that set that variable name `X-CCFlare-Path`, which this tree parses nowhere** — including this repo's own `.claude/settings.json`. That is `SB23-2268`, and the fix is to accept it as a cwd-bearing alias here rather than edit nineteen repositories. Also: the 164 attributed rows no longer exist, because retention prunes at three days, so "do the 164 still join" is now unanswerable rather than negative
- **Database backups were 0644 with plaintext tokens until `#140`.** `restrictDbFiles` swept only `-wal` and `-shm`; `restrictDbBackups` now sweeps `<db>.backup.*` too, including `.partial`, `-wal` and `-shm`. The creation site was already correct as of `#76` — backups are written by `VACUUM INTO`, whose mode comes from the **umask**, not from the source file, so a 0600 database still produced 0644 backups under umask 022. **The host's backups were still 0644 a day after `#140` merged, and the reason is the reusable part.** v3.22.0's tag `b9852647` **predates** `#140`, so the running binary has no `restrictDbBackups` and the promised restart-sweep was never going to happen. Six files (two `.backup.` plus their `-wal` and `-shm`) measured 0644 at 2026-09-18 22:17Z and were brought to 0600 by hand. **A merged fix is not a deployed fix**: before writing that a restart will handle something, name the release that carries it and the version the host runs. See `mem:merged-is-not-deployed`
- **`#131` merged with its review covering only its first head, and this is the shape to watch for rather than a one-off.** The reviewer saw `787dd653`. Two pushes followed, `a5337846` then `50b31489`, and a branch update produced the merged head `e056fcdf`. So every fix made *in response to* the review is unreviewed, including the one that made finding 7 reachable before fixing it. The two real defects that review found are worth knowing on their own: a reset threshold of `0` silently disabled the slot, on the one field with no warning, and `parseHoursField` had no upper bound, so `1e20` hours is `3.6e26`, `Number.isInteger` says true, and the value passed the form, passed the handler and reached the database above int64. `SlotSettingsPopover.tsx` has no tests and has never been opened in a browser
- **`SB23-2061` bounds `min_reset_remaining_ms` in `packages/http-api/src/handlers/combos.ts`, and its first step is not the bound.** `MAX_RESET_HOURS` has to move to `packages/core` or `packages/types` first, because `packages/dashboard-web` cannot import `packages/http-api` without pulling `database`, `proxy` and `providers` into the browser bundle — the same constraint that moved the route catalog in `#106`. Move the constant, bound the handler against it, then have the UI read it instead of its own copy. `SB23-2062` was filed against the same handler by a session that had not seen `SB23-2061` and is canceled as a duplicate; **two agents filing the same follow-up an hour apart is what happens when a peer's close-out names a follow-up and the orchestrator files it without searching first**
- **Read `mem:layout-measurement-needs-three-filters` before measuring any layout in a running page.** `SB23-2058` produced 1099px of overflow, then 358px, then the real answer: **−9.0px**, nothing reaching a card border. Zero-size nodes put their rect at the origin so the card's own left edge counts as overflow; the Radix progress indicator is translated left of its track and clipped by it; and `resize_window` **reported success three times while `innerWidth` never changed**, then collapsed the window to 0 width, where every flex row wraps and a reading is indistinguishable from a regression. One thing `#133` changed and nobody has looked at: narrow list view, roughly 1000px with the sidebar expanded, where the controls now drop to a second line and `justify-between` leaves that lone wrapped line **left-aligned rather than flush right**
- **`GET /api/config` under-reported session duration by a factor of five on every install until `#128`, and had a passing test throughout.** `config.ts` read `settings.sessionDurationMs` off `getAllSettings()`, which spreads the **snake_case** config data, so the property was always `undefined` and the endpoint fell through to the one-hour `ANTHROPIC_SESSION_DURATION_FALLBACK` while the session logic used the five-hour `ANTHROPIC_SESSION_DURATION_DEFAULT`. **The mock in `config.test.ts` supplied a camelCase key that exists in no real response**, so the test agreed with the defect rather than catching it. It now reads `config.getRuntime().sessionDurationMs`, and `ANTHROPIC_SESSION_DURATION_FALLBACK` has zero consumers and is deprecated. **A mock's shape is an assertion about production**: check its keys against what the real producer returns, especially at the snake_case/camelCase boundary
- **`db_page_size` can never apply, on any database, and `#129` only warns about half of that.** Four things, all measured against `bun:sqlite`: SQLite discards a value that is not a power of two in 512 to 65536, with no error; it also discards a **legal** value on any database holding data, staging it until a full `VACUUM`; **`journal_mode = WAL` runs first inside `configureSqlite` and allocates pages**, so even a database created microseconds earlier is already non-empty; and `pageSize` defaults to `2048` whether or not an operator set anything, while SQLite's default is 4096. The invalid-value warning shipped. **The staged-value warning was written, tested, then withdrawn** because finding four means it would fire on every boot of every install for a setting nobody chose, and the suite emitted it 95 times in one run. Making it work needs the PRAGMA moved above WAL, which is a behaviour change on database initialisation
- **Read `mem:covering-the-function-is-not-covering-the-call` before trusting a mutation result.** It fired twice on 2026-09-14 and a mutation survived both times: the page-size warning could be deleted with every test green, and the retry card's ceiling could be swapped for a local constant with all eleven of `retryWaitCeilingMs`'s tests green, because the argument handed to the function was covered by nothing. **Write one mutation against the call site for every mutation against the function**, and make the killing assertion a negative one
- **`SB23-1269`'s semantics are settled, and `#107` had them wrong.** Sean ruled 2026-09-14: only the **configured** conditions are considered, and every configured one must hold. `#107` required **both** columns before the rule fired at all, so a half-configured rule was inert. `#112` (`be3a9eb7`) corrects it: return false only when **neither** threshold is set. The comparison direction is unchanged and confirmed, `resetMs - now >= min`, skip when the reset is still far away. A `resetMs` in the past disqualifies the snapshot as stale; an absent `resetMs` ignores the reset clause so a utilization-only rule still fires. **The UI half is still open on `SB23-1269`**, and it must offer the two fields **independently** rather than as a pair; also open there is that `slot.enabled` is writable through the repository and API and honoured by the selector at `:713` but exposed by no UI
- **The three `retry_*` keys are wired as of `#113` (`8c0f6092`), and the rule is that only a throw is retried.** `retry_attempts` / `retry_delay_ms` / `retry_backoff` were parsed twice, printed by the CLI as live, documented in nine places and read by no retry logic. `forwardWithTransportRetry` (`packages/proxy/src/handlers/upstream-retry.ts`) now retries them. **No status code is retried**: a throw means no `Response` existed, so nothing reached the client and the upstream never answered, which makes the never-retry-a-streamed-response rule automatic and cannot go stale as status codes change. 429 stays with the account selector, 529 keeps its own in-place retry. A throw alone is not enough either, because the guarded call wraps observation and header handling, so the predicate is an **allowlist of connect-phase and TLS error codes** with aborts excluded. **These keys drive two in-place retries, not one**: the 529 loop and the ZAI 1305 case, an overload inside an SSE body on an HTTP 200. The two layers can overlap on one request and the worst case is attempts squared, 9 upstream fetches at the default 3, reached only by a 529 followed by repeated connect failures. **The root cause was not the missing consumer**: `apps/server` built `RuntimeConfig` with `config.get(key, default)` per field, `get` reads the config file alone so all five env vars were discarded, and `get` **persists** its default on a miss, so first boot wrote `retry_attempts: 3` to disk and the file then outranked the environment permanently. It is `...config.getRuntime()` now. One behaviour change on upgrade, disclosed: a default install's 529 loop moves from 2 attempts at 750ms base to 3 at 1000ms
- **Read `mem:validation-fallback-picks-the-aggressive-end` before adding a numeric config key.** `retry_attempts: 0` silently became **3**, the largest value in play, because the consumer validated with `>= 1` and otherwise took the default. An operator asking for no retries got the most retries. Nothing upstream caught it because `retry_*` is accepted on a bare `typeof === "number"` while the adjacent `db_retry_*` goes through `validateNumber` — two families in one file, one checked. Tracked as `SB23-1980`
- **`projects.canonical_path` is no longer built by inverting the Claude slug, as of `#116` (`76fad88d`, `SB23-1975`).** The slug maps `/`, `_` and `.` all to `-`, so the inversion was never a function and 530 of 604 live rows pointed at paths that do not exist. `resolveEncodedName` now descends the real filesystem and encodes each directory **forward** to compare; a name no real directory encodes to returns null and the entry stays `ambiguous`, and two siblings that encode alike return null rather than guessing. `naiveDecode` survives as a last resort and is documented as wrong by construction. **A row at or above the home directory is excluded from the prefix index** (`isCatchAllPath`), because `/users/seanbugeja` would otherwise prefix-match every request and turn null attribution into uniformly wrong attribution. The row stays in the database and an explicit worktree parent rule still resolves to it; only the prefix match is removed, and `homeDir: null` restores the old behaviour
- **Four things about that fix that are true and are in no commit message.** (1) **`SlugRuleDriftError` cannot fire on the 530-row defect it was written for**: entries carrying a JSONL `cwd` never reach the forward resolver, so on a host where most projects have transcripts the guard stays silent however wrong the rule is. It catches a *drifted slug rule*, which is a different failure. (2) **`MAX_VISITS = 20000` is a round number, not a measurement**; hitting the cap returns null, which is indistinguishable from a genuine miss, so an unexpectedly large `ambiguous` population should indict the cap before the rule. (3) **`requests.project_id` has no foreign key** to `projects.id`, so re-keying a project detaches its history with no constraint violation and no log line. (4) **Deleting or disabling the home row does not work and is the first thing a reviewer will propose**: discovery re-creates it on the next scan because it is a real directory with a real transcript directory, so the exclusion has to live in the resolver
- **A project's id is `sha1(canonical_path).slice(0, 16)`** (`packages/database/src/repositories/project.repository.ts:11`), so **any rewrite of a stored path re-keys the row and orphans its `requests`**. The stored paths are lowercased: `/users/seanbugeja/code/runpod_workspace` hashes to `084f767d0b7bbb27`, the real-case form to `ddb001cb08dd347f`. What keeps ids stable is `PROJECTS_CASE_SENSITIVE` being false on darwin, which makes `discovery-scheduler.ts:125` lowercase the resolver's real-case output. **Flipping that setting on a populated database re-keys every project row**, silently. Tracked as `SB23-1988`; the rebuild against live data is `SB23-1985`, the third `forwardToClient` site is `SB23-1986`, the stale plan line is `SB23-1987`. **`SB23-1988` is fixed as of `#119`**: the boot refuses when the setting would re-key a populated table, recording the mode the rows were built under in `projects_case_sensitive_stored`. The marker is derived from the rows (`rowsHaveUppercase ? true : current`) rather than from the current setting, because an operator who flips the setting and *then* upgrades would otherwise have the guard adopt the new value and stay silent. One quadrant stays open and is documented in the code: all-lowercase rows with the setting at true cannot be told apart from genuinely-lowercase directories without the filesystem
- **The forward-slug fix is verified on real data, and the acceptance line that looked like the proof was wrong.** `SB23-1985`, run 2026-09-14 against a copy of the live database. **72 paths repaired**, recovering all three collapsed separators: `code//mcp/proxy` became `code/.mcp-proxy`, `runpod/docker/image` became `runpod_docker_image`, `eeg/ggai/ggleap/workspace` became `eeg/ggai-ggleap-workspace`. **Zero forward-resolved paths point at a missing directory** and all 32 doubled-slash entries are flagged `ambiguous` rather than asserted. **Do not measure this as "zero rows fail `existsSync`"**: 430 entries are confidently and correctly asserted from the JSONL `cwd` while their directories have since been deleted, which is accurate history and would read as failure. The number that carries meaning is how many paths the forward resolver placed that do not exist, and it is zero
- **Attribution works end to end, proven by live traffic rather than a scripted request.** 164 `requests` rows now carry a non-null `project_id`, against zero ever before 2026-09-14: 156 to `/users/seanbugeja/code/claude-meta-worktrees/1296` and 8 to `/users/seanbugeja/code/runpod_workspace`, both joining to real `projects` rows. **The 542 broken rows are untouched** and stay that way until the host upgrades past 3.10.0 and the scheduler runs the new scan, so the rebuild is a consequence of the brew upgrade rather than a separate task
- **Decide whether a PR earns a reviewer by counting non-test, non-comment added lines, and by nothing else.** `git diff --stat` and `gh pr view --json additions,files` both count tests and comments, which are the two things a PR should be **rewarded** for carrying, so raw additions punish the well-documented diff and wave through the dense unexplained function that most needs reading. The command is:
  ```sh
  git diff origin/main...HEAD -- ':!*.test.*' ':!*__tests__/*' | grep -E '^\+' | grep -vE '^\+\+\+|^\+\s*(\*|//|/\*)' | grep -vE '^\+\s*$' | wc -l
  ```
  **Two things in that command were wrong until 2026-09-19 and both undercounted.** `':!*test*'` is a **substring exclusion over the whole path**, not a match on test files, so it also drops `testing/`, `testbed/`, `latest/`, `attestation.ts` and anything else carrying those four letters anywhere. It read **2** logic lines against a real **57** on `#185`, which added `packages/security/src/testing/`. And `refs/heads/main` is stale in every linked worktree, because `main` is checked out in the main checkout and no worktree can fast-forward its own copy; a lane measured **39 lines across four files** against it where the truth against `origin/main` was **21 across three**. **Undercounting is the dangerous direction**: the exclusion exists so tests and comments cannot push a diff over the threshold, so an error in it waves work through, and it does so on exactly the diffs that add a new directory. **The detection is cheap**: compare the filtered `git diff --stat` against the unfiltered one and look at which files the pathspec actually kept. A count that looks small against the diffstat is the instrument, not the diff.
  `#121` was briefed as "a card plus one handler", measured 756 additions across 9 files with a new public route, and the review its author nearly skipped found a real defect. `#126` measured 470 additions across 7 files but **62 logic lines**, roughly 300 of the rest being two new test files.
- **The threshold stays mechanical. "No new public route, no security surface" is not an input to it.** That sentence is true of most PRs that needed a review, and it is what every author believes about their own diff at the moment they decide to skip one. Ruled by BTCF-2040 against BTCF-000, who had reached the right number by the right method and then justified skipping with exactly the self-assessment that failed on `#121`. A mutation record is evidence; the author's description of the blast radius sitting next to it is not. **Let the metric be occasionally wasteful**
- **A number written into an issue body is treated as measured by every lane downstream.** `SB23-1972` said a backoff of 2 with 5 attempts and a 1000ms base is 15 seconds of waiting. It is **11 seconds**: `retryDelayMs` is `Math.min(baseMs * backoff ** attempt, maxMs)` called with `attempt + 1`, so the exponent starts at 1 and every cap is clipped at 3000ms. The wrong figure reached the PR body, a code comment and the card, and **four agreeing statements with one origin are indistinguishable from corroboration**. The card's test now restates `retryDelayMs`'s formula rather than asserting the number, because copying the card's output would have pinned the bug. The understating case is the dangerous one: at a 100ms base the same error read 1.5s against a real 3s, and understating a wait is what makes an operator raise it
- **`claudedocs/projects-worktree-feature-plan.md:205` is stale and has misled two sessions.** It presents `X-CCFlare-CWD` as a proposal; it shipped and is read at `packages/proxy/src/handlers/request-handler.ts:44`. One of the two wrote the belief into a peer brief as settled fact, across a repo boundary. Correcting that line is acceptance item 2 on `SB23-1975`. The general rule is `mem:stale-plans-generate-false-facts`: a planning document is evidence of intent, never of absence, so grep the source before asserting a feature is unimplemented
- **There are two features called throttling and they are not the same.** `getUsageThrottleStatus` (`packages/proxy/src/handlers/usage-throttling.ts:259-314`) compares utilization against the **expected pace** of a window, is driven by two **global booleans**, runs **after** selection, and **529s the whole request** when every candidate is throttled (`packages/proxy/src/proxy.ts:620-621`). The per-slot thresholds from `#107` live **inside** the combo slot loop and **skip to the next slot**. Conflating them is the obvious mistake
- **`GET /api/meta/routes` serves the catalog, and the catalog now lives in `packages/types/src/api-catalog.ts`.** It moved out of `packages/dashboard-web` in `#106` so the server can import it; putting it in `packages/http-api` instead would have pulled `database`, `proxy` and `providers` into the browser bundle. The drift guard now covers dynamic routes as well as static, **by prefix containment in both directions, which is weaker than the static check** and says so in its own header: it does not catch a new dynamic route under an existing prefix, a wrong method, or wrong tail segments. **Any new route must be added to that catalog in the same commit** — only the full suite runs the guard, so a focused suite passes
- **Sean's install is eight releases behind and his database is 0644 because of it.** Observed 2026-09-14, read-only, no maintenance window needed: the listening process on 8080 is pid 66391, `lsof txt` resolves it to `/opt/homebrew/Cellar/better-ccflare/3.10.0/bin/better-ccflare`, and `brew list --versions` agrees, so **there is no binary-versus-Cellar skew** — that gate on `SB23-750` is now green. But `better-ccflare.db`, its `-wal`, its `-shm` and two backups are all **0644** holding plaintext tokens, because PR #76's fix shipped in v3.15.0 and the running binary predates it. **The exposure is bounded, not critical**: the containing directory is `0700`, so another non-root user cannot traverse in. The config file is already 0600. The remaining `SB23-750` gate is the schema-versus-binary comparison, which the upgrade would also settle
- **`/v1/models` truncated stitching and the catalog's replace rule are load-bearing on each other.** `ingestModelsListing` (`packages/proxy/src/model-catalog.ts:442`) treats `body.has_more !== true` **and** no client `after_id` as a complete listing and **replaces** the catalog; anything else merges. So `#100` returns `has_more: true` on every non-clean exit (page cap, failed page, timeout, non-200, unparseable). **Tidying that to `false` would silently start replacing the catalog with a partial listing and nothing would fail.** If you change one, change the other. There are now two `after_id` loops in that area and they are deliberately not shared: `fetchLiveModels` (`:213-277`) is the background refresher on its own schedule and may fail a whole refresh on a bad page, while the request path must still return a usable body to a waiting client
- **The OpenObserve log stream defaults to `INFO` as of `#103`.** Before it, every level shipped, and since the exporter's buffers are bounded and drop the oldest under pressure, a `DEBUG` burst could **evict the `ERROR` records that were the reason for shipping logs at all**. `openobserve_log_min_level` or `BETTER_CCFLARE_OPENOBSERVE_LOG_MIN_LEVEL`; `DEBUG` restores the old behaviour. An unrecognised level falls back to `INFO` and warns rather than shipping nothing, because a typo would otherwise look exactly like a working exporter with nothing to say. The warning deliberately avoids the shared throttle: a bad level is on every log line and would consume the 60-second window the exporter's real failures need. **Adding any OpenObserve config key touches five files**, and the easiest to miss is the hardcoded field list in `packages/http-api/src/handlers/config-openobserve.ts` — miss it and the dashboard silently cannot set the key. No UI selector for the level yet; it is settable by API, config file and environment only
- **Formatting is a CI gate as of `#135` (`e49b7d08`), and `bun run lint` is not the command to satisfy it.** `bunx biome check .` runs as a step inside `Bun and release tooling`, failing on errors only; the 30 warnings and 4 infos left in the tree are deliberate and nothing is suppressed. Before opening a PR run **`bunx biome check --write <your files>`**, scoped. **Never `bun run lint`**: it is `biome check --write --unsafe`, which mutates the tree and can exit 0 having rewritten code, so it reports nothing and cannot tell you whether CI will pass. `packages/dashboard-web/dist/**` and `**/inline-*worker.ts` are excluded at `biome.json:13-14`, so a fresh build cannot fail the gate, and `scripts/ci-changed-paths` treats `biome.json` as relevant so a config change still runs the step
- **An earlier version of this line claimed zero biome errors as of `6c22fadb`. That was true for about a day.** Measured at `b9852647` before `#135`: **three errors**, and clearing them exposed **two more**. All five were in files written on 2026-09-13 and 14 for the `SB23-1975` forward-slug work — two `useTemplate` in `project-resolver.ts`, one in `discovery.test.ts`, unsorted imports in `forward-slug.test.ts`, formatting in `project-resolver-catchall.test.ts`. **A "currently zero" count in a document is a measurement with a shelf life, not an invariant**; the gate is what makes it an invariant. `biome.json:14` itself trips `useBiomeIgnoreFolder` as an info and is left alone, because changing that ignore syntax changes which files get checked
- **Never trust a `timeout` option on a synchronous spawn.** `Bun.spawnSync`'s `timeout` cannot fire, because a synchronous spawn runs no event loop. `#102` found one that had been written, reviewed and merged while doing nothing, on a child blocked in `open()` on a FIFO — which is exactly the case it was there for. That fixture leaked a process for **5h44m** at 0.0% CPU. Two rules from it: **killing a process is not killing its descendants** (two pids were killed and confirmed gone while a grandchild reparented to `init` and ran on), and **verify death and parentage from a later tool call**, never at the moment of acting, since a just-signalled process still appears and a just-orphaned one still shows its old parent. Count a zombie as gone or the cleanup hangs on the tidy case. Full detail in `mem:bound-runs-with-timeout-k`
- **The Anthropic peak-hour error ran March to November, not November to March.** `SB23-1867` filed it with the two halves of the year inverted and its acceptance criterion would have encoded a fresh one-hour error. Measured: `2026-01-15T13:00:00Z` is `05:00 PST` and `2026-07-15T13:00:00Z` is `06:00 PDT`, so the old fixed UTC 13:00-19:00 constant was correct through winter and an hour late through PDT. Los Angeles is UTC-8 on PST, and subtracting the larger offset from a fixed UTC hour lands earlier in local time, not later. **A Los Angeles window cannot pin the weekday rule**: LA is behind UTC, so its 5-11am window never crosses a UTC date boundary and the local and UTC weekday rules agree by coincidence. `#93` pins the mechanism with a synthetic window in a zone ahead of UTC instead
- **`status.claude.com` is an Atlassian Statuspage**: `GET /api/v2/{status,summary,components}.json`, 200 JSON, no auth. `#95` filters to `Claude API (api.anthropic.com)` and `Claude Code` and deliberately does **not** use the top-level `indicator`. At the time of writing it read `minor` because of a Windows-only Claude Cowork incident, which would have been a false alarm on day one. A missing watched component id is reported rather than ignored, because a filter that silently matches nothing renders no banner, which is indistinguishable from "all operational". `BETTER_CCFLARE_SERVICE_STATUS_REFRESH_SECONDS=0` disables the poller
- **The circuit breaker reports but does not gate, and that is deliberate.** Before `#96`, `recordFailure` and `recordSuccess` were wired into the live proxy path while `shouldAllow` and `isProviderWideOpen` had no non-test caller, so state accumulated and nothing read it. The module's own header said it was unwired, which is how that survived. `#96` adds a snapshot on `/api/health`, injected so an open circuit cannot alter the endpoint's status code or its `pool` counters, and fixes the header. **Gating was rejected, not forgotten**: it would add a fourth exclusion beside `rate_limited_until`, `requires_reauth`, `paused` and usage exhaustion, so a wrongly-open breaker could empty a pool that would otherwise route. Whether to gate is still open and is Sean's call
- **Read `mem:tests-that-pass-for-the-wrong-reason` before writing any test that asserts an environment-dependent value.** Three instances landed on 2026-09-13 and 14: under umask 077 `mkdirSync` yields 0700 and SQLite yields 0600 with no help from our code, so file-mode assertions pass against reverted source; the Los Angeles weekday coincidence above; and a guard made redundant by a second guard on PR #76. The rule is to write the mutation before believing the test, and to prefer the case where the two mechanisms disagree
- **The `restrictDbFiles` ordering is kept for a race window, not for the resting mode.** Corrected 2026-09-14 by mutation M4 on `#91`, which moved the call below `configureSqlite` and **survived**. `restrictDbFiles` sweeps `-wal` and `-shm` explicitly (`packages/database/src/file-modes.ts:57-61`), so the reversed order still lands 0600 on disk; only the window in which the WAL exists at 0644 differs. An earlier version of this file and of `mem:account_credentials_and_file_modes` implied a permanently 0644 WAL, which was wrong. No test asserts the ordering and none can without racing the window, so do not "restore" it on the strength of a green suite. Also measured there: the `DatabaseOperations` **constructor** is cheap to test — workers, heartbeat and the multi-instance guard all live in `initializeAsync()`, so a constructor-level test needs only `DATABASE_URL` cleared and `XDG_CONFIG_HOME` pointed at a temp tree. `SB23-1812` deferred work once on the opposite belief
- **`SB23-1522` was rescoped on 2026-09-14 and is now a 1, not a 2: token usage already ships to OpenObserve.** The `summary` literal at `packages/proxy/src/usage-collector.ts:1020-1065` carries ten token and cost fields and is spread wholesale into `shipRequestRecord` at `:1067-1074`; the exporter takes a loose `Record<string, unknown>` and is field-agnostic, so they arrive with no exporter change. Do not build a second shipping path. What remains is a test — `packages/logger/src/openobserve.test.ts` currently calls `shipRequestRecord` only with `{ id: "req-1" }`, so deleting a token field would be silent — and documenting four things that mislead: shipped names are camelCase while the columns are snake_case and OpenObserve lowercases on ingest; absent token fields are **omitted, not zeroed**, so an `avg()` over the stream is not the average anyone means; `_timestamp` is request end while the `timestamp` field is request start, and OpenObserve indexes the former; and there is no retry, so the stream is lossy under pressure and is never a billing record
- **Bound every long command with `timeout -k 10 <n>`, never bare `timeout`, and stop runs by pid.** Measured 2026-09-14: `timeout 120 bun test packages/config/src/config-non-regular-read.test.ts` sat for **three hours and six minutes**, because the child was blocked in a FIFO read where SIGTERM has nowhere to land — it was not ignoring the signal by choice. The wedged file was the FIFO test itself, the one that exists because `readFileSync` on a FIFO blocks forever, so a fixture FIFO destroyed the test written to catch that defect. `pkill -f "bun test"` is machine-wide for a bare invocation and on the same night killed suites in unrelated repos; a killed suite still prints a plausible summary, so treat a suite line as valid only when pass + skip + fail sums to the total it reports. Full detail in `mem:bound-runs-with-timeout-k`
- **Read `mem:account_credentials_and_file_modes` before touching accounts, config paths or database files.** Three things that are invisible from the field names: an API key is written to `api_key`, `refresh_token` **and** `access_token` at creation, so a rotation touching one leaves two stale copies and breaks auth while passing any test that reads `api_key` back; the config-directory chmod is gated by string equality against `getPlatformConfigDir()` because `BETTER_CCFLARE_CONFIG_PATH=/etc/…` would otherwise take `/etc` to 0700 and lock every user off the machine; and SQLite's unix VFS gives a new WAL the main file's mode, so the database is chmodded before `configureSqlite` rather than after — though see the `restrictDbFiles` entry above for what that ordering does and does not buy
- **Shipping to OpenObserve works and was proven live, not assumed.** App logs come off `logBus`; request and response payloads are shipped without ever reaching the database when `store_payloads` is off. Off by default — a configured base URL is the switch, and `openobserve_ship_payloads` separately decides whether records carry bodies. Configure it from the dashboard (`GET`/`POST /api/config/openobserve`) or by environment variable. **Read `mem:openobserve_telemetry_seams` before touching any of it**: it records the two gates that both had to widen, the `logBus` move to `packages/logger/src/log-bus.ts`, and four OpenObserve behaviours that mislead — in particular that a stream's `doc_num` lags so badly it reports a working exporter as dead, so verify with `POST /api/{org}/_search` and never with stream counts. Open follow-up: `SB23-1760` (retry, log min-level filter, per-account attributes, metrics, traces, OTLP, disk queue). `SB23-1769` is fixed — `biome.json` now excludes `**/inline-*worker.ts`, so `bun run lint` can no longer rewrite the generated workers, though it is still repo-wide and still mutating
- **`SB23-1296` is stopped, not started, and needs a decision.** The receiving half already exists: `x-better-ccflare-project`, the `X-CCFlare-Project` alias and legacy `x-project` are parsed at `packages/proxy/src/usage-collector.ts:135-153`, with `project_id` and `worktree_path` columns on `requests`. What could not be found is the *sending* side the issue refers to as "the hook work" — no `ccflare` reference in `claude-meta/hooks/`, no `ANTHROPIC_CUSTOM_HEADERS`, no shipped hook. The only description is a plan at `claudedocs/projects-worktree-feature-plan.md:205` proposing `X-CCFlare-CWD`. So the issue is either already satisfied or it is host-level work in `claude-meta`. Do not guess; the full measurement is in a worklog comment on the issue
- **`.mcp.json` is gitignored here as of `048b8631`, and that is deliberate.** The format stores credentials as literal header and env values rather than `${VAR}` expansions. Measured across seven repos on this machine: every repo that tracks one has live keys committed as literal values, including `SijanC147/claude-peers-mcp` at `df23ada`. **Correction, 2026-09-13:** an earlier version of this line called that repo public and flagged it as a live leak. It is **private** — `gh api repos/…` reports `private=true`. The first reading came from a bare `gh repo view --json visibility` with no repo argument, run inside a loop that had just `cd`-ed, and it was reported as established fact without a second look. The credential half was independently verified and stands; only the severity was wrong. See `mem:escalation-needs-rung-four`. Never track this file regardless.
- **v3.10.0's Homebrew publication is resolved and no longer outstanding.** It failed on the tap's own `Published Formula gate`, not on anything in this repo: the gate lints the whole tap and `Formula/headphones.rb:325` used `def post_install`, which Homebrew now rejects. HTAP-000 fixed it (tap `main` green, three blockers not one), and every release since has published cleanly. Kept here because the diagnosis is reusable: a `Publish Homebrew Formula` failure is usually the tap, not the release, and `Formula/better-ccflare.rb` carrying the right URLs and sha256s is the evidence that the release itself completed. Recovery for a homebrew-only republish is `gh workflow run hextap-release.yml --repo SijanC147/better-ccflare --ref main -f tag=<tag>`. Three fix routes that do NOT work, measured: inline `rubocop:disable` is banned by `Style/DisableCopsWithinSourceCodeDirective`; a tap `.rubocop.yml` is ignored because `brew style` forces `--config`; the steps-DSL migration clears the offence then trips on `#{HOMEBREW_PREFIX}` inside `args:`
- **The config file is 0600 as of PR #57 (`f2dc5c8e`), tracked as `SB23-1709`.** It held `pg_password`, `local_control_secret` and the maintainer PAT at 0644. Fourteen review rounds found six further reachable defects, each one introduced by the previous round's fix: an authentication bypass through an ungated read, an arbitrary file overwrite through unchecked intermediate symlink hops, a config path naming a directory losing its execute bits (which locks the operator out of `better-ccflare.db` in the same directory), ownership discarded by the rename, dangling and multi-hop links destroyed, and `/etc` plus bind-mount layouts silently ceasing to persist. Three mutation rows survive and are disclosed in the commit rather than buried: ownership preservation, the Windows gate, and fsync, each needing root, a second account, or a Windows host. Open follow-ups: `SB23-1700` (regular-file config injection plus the FIFO that hangs startup forever), `SB23-1696`, `SB23-1686`, `SB23-1679` (the 0644 `better-ccflare.db` with plaintext tokens beside the config — a bigger exposure than the file #57 fixed), `SB23-1680`
- **The upstream maintainer is live and has completed a real sync, tracked as `SB23-1645`.** PR #52 merged the first App-produced sync: 106 commits, 121 files, `4d27cb22..ea0e3320`. The fork is level with upstream. All 15 gates passed first time with zero repair rounds and the reviewer approved on specifics. Read `mem:upstream_maintainer_gates` before editing `.github/upstream-maintainer.yml`: `protected_paths` alone protects nothing, `forbidden_patterns` reads only the upstream tree, and `*` never crosses a slash in a glob
- **Three triggers, all proven: the Monday schedule, manual dispatch, and a webhook.** The webhook is `repository_dispatch` with `event_type: sync-upstream` and a `client_payload.target` filtered against `TARGETS`. newreleases.io can POST GitHub's dispatch endpoint directly, so no relay is needed; upstream publishes ~9 releases a week, which is a sharper signal than the cron
- **PR #55 turned on version proposals and draft releases, and neither has ever run.** A merged sync PR now drafts a release. **Publishing that draft is irreversible**: it creates the tag at the pinned commit, fires `hextap-release.yml`, and `v*` tags cannot be moved or deleted. The order inverts from the old hand flow — draft first, THEN the manifest-bump PR, then retarget the draft, then publish. `version.kind` must stay `fork-semver`: Hextap rejects any tag containing `+` at source, so the upstream-derived scheme would fail the release on an undeletable tag
- Still unexercised anywhere: the repair path, feedback rounds (`FEEDBACK_ENABLED` unset), and the whole draft-release path. The first sync after upstream publishes is the test
- Three controller defects were found only by running it, all fixed in `SijanC147/upstream-maintainer`: the target branch was fetched by bare name and this repo has both a `main` branch and a `main` tag, so every run aborted in `prepare` (#34); agent stdout was capped at 1 MiB, which a 106-commit review exceeded (#35); and `agent.max_repair_rounds` plus `agent.timeouts.repair_minutes` were parsed and then consumed by nothing, so every agent session was killed at 8 minutes regardless of config (#36). Treat a config key as inert until you have found its consumer in the controller's source
- CI skips its expensive steps when nothing that can change a result changed (PR #49). `scripts/ci-changed-paths` decides, the trigger is deliberately NOT path-filtered because both job names are required checks and a skipped workflow never reports them, and steps are gated on `!= 'false'` so a missing output still runs everything. Measured: a `.github/**` comment-only PR finishes `Bun and release tooling` in 8 seconds, green
- Backlog: `SB23-313` (28 pre-existing biome lint errors), `SB23-314` (non-deterministic `bun test` exit code — downgraded to Low 2026-08-28; the original evidence compared different checkouts rather than repeats in one tree, and the exit code is now consistently 1, correctly reflecting the two macOS-only failures) — both non-blocking tech debt
- Two tests fail on macOS only and pass on Linux CI: `AgentRegistry — injected workspace persistence` (both cases). Cause is macOS `/var` → `/private/var` realpath, not a defect. Do not "fix" them by loosening the assertion
- Upstream sync: last completed PR #52 (2026-09-13, 106 commits `4d27cb22..ea0e3320`), the first produced by the App rather than by hand. Next anchor: `last-sync-sha ea0e3320` in `MERGELOG.md`, which the maintainer now advances itself. Syncs are the App's job now; `/sync-upstream` is the manual fallback. PR #35 (raw `tombii:main → main` pull) was closed unmerged — it would have dropped the Hextap files; never merge that bot PR
- `/sync-upstream` is a **user-scoped, repo-agnostic** command; this repo's specifics live in tracked `docs/upstream-sync.md` (remotes, ref-ambiguity, protection, commands, generated files, inviolable Hextap paths, standing conflict resolutions, and the "breaks without conflicting" audit). Read that before any sync
- `brew hextap validate --build` refuses unless the local Bun is **exactly 1.3.14** (CI pin). A newer local Bun can still run every other gate step — the five-target compile is proven by cutting an `-rc.N` tag instead

- **Five PRs merged 2026-09-18 from four parallel lanes**, `main` at `f0d7b0ca`: `#142` the stale plan line (`SB23-1987`), `#143` `GET /v1/models/{id}` (`SB23-1969`), `#144` the OpenObserve retry slice (`SB23-1760`), `#145` the two config-permission follow-ups (`SB23-1686`, half of `SB23-1696`), `#146` the account renewal day (`SB23-2055`). **Every lane overruled the orchestrator at least once and was right each time**, which is the strongest argument in this file for the independent-review cost
- **The merge gate is CI's run at the merged head, always, and there is no carve-out.** `hextap/main` requires an up-to-date head, so every merge invalidates every other open PR and each one needs `gh pr update-branch`; that moves the head, so **the peer's full-suite line no longer covers what merges**. It is still evidence about the peer's work and it is still the only run whose failures got named rather than counted. The hazard is not what the PR changes, it is **what landed in between**: a config-only PR merged onto a main that just changed a config key's validation is the same hazard with none of the warning signs. A carve-out for "safe" changes reintroduces the judgement call the rule removes, and it gets exercised by whoever is most confident
- **`git reset --soft <branch>` is unsafe in a worktree, full stop, and `git add -A` is the half that does the damage.** It happened **twice in twenty minutes** to one lane, the second time twenty minutes after that lane wrote the memory about it. First it staged the pre-`#142` copy of `claudedocs/projects-worktree-feature-plan.md` as a **revert of a merged PR**, clean, no conflict, green CI, under a `feat(logger)` title. Then it staged three files from the just-merged `#143` the same way. What caught the second was the push being rejected as stale because the orchestrator had run `update-branch`: not discipline, not a check. **`git add <paths>` alone prevents both**, and the detection command is a merge simulation, because `git diff main...HEAD` (three dots) is merge-base relative and structurally blind to it while two dots false-positives on every behind branch. See `mem:reset-soft-reverts-what-moved-under-you`
- **The `accounts` migration rule is TEN places, not seven, and the last three fail differently.** Beyond the seven this file already named, three explicit **read-side** SELECT lists exist with nothing pointing at them: `AccountRepository.findAll`, `AccountRepository.findById`, and the accounts list handler's own SELECT. A mutation deleting a column from the repository pair **survived every suite** until a test was added. The distinction matters: a rebuild list **drops** the column and destroys data, while a read-side list merely never reads it, so the data survives intact and the application behaves as though the operator never set a value. Both are silent; only one is lossy, and a reader who conflates them will think a backfill can repair the second
- **CI runs in UTC, so a whole class of defect is invisible to CI and to everyone east of Greenwich, Sean included.** Swapping `getUTCDate()` for `getDate()` in a `daysInMonth` helper **survived** under UTC and under `Pacific/Kiritimati` and died only under `America/Los_Angeles`, because for a midnight-UTC instant the two agree in UTC and in every zone east of it. The shipped code was correct; the hole was structural, and no test closes it without pinning a timezone the suite does not control. The fix was to **remove the zone-dependent call**, not to test around it. That trade was not free: hand-writing the leap rule created a `% 400` branch no test reached, and deleting it survived the whole focused suite
- **Prefer the construction that cannot express the mistake over the check that would catch it.** Four independent instances in one evening: `git add <paths>` over remembering not to `add -A`; a constructor that throws on an absent path over a brief saying do not write; `MAX_BATCH_ATTEMPTS` over hoping an outage is short; a month-length table over a zone-dependent `Date` call. **A check is a habit, and three habits failed that night while their authors could recite the rule**
- **A merged fix is not a deployed fix.** Six database backups sat 0644 with plaintext tokens for a day after `#140` closed that issue, because v3.22.0's tag predates the fix and the note said a restart would sweep them. Before writing that a restart will handle something, name the release that carries it **and** the version the host runs, then re-measure the artifact rather than the code. See `mem:merged-is-not-deployed`
- **An acceptance criterion offering "or state that X does not exist" is the clause most likely to be wrong**, because it is the only one a lane can satisfy without measuring anything. `SB23-2055` let a lane declare the PostgreSQL migration path untested because no harness exists. One does: `migrations-pg-role-backfill.test.ts` drives `runMigrationsPg` through a fake adapter that records the SQL it would emit. The issue's evidence section was careful, cited `file:line` throughout, marked its uncertain claims unproven, and still got this wrong, because its author searched for a Postgres **database** harness and there is none. Falsifying it cost four minutes and produced a test that then caught a real defect
- **A third read-only sub-agent wrote through a fixture whose path resolved wrong, and this one reached the live production config.** `#145`'s security-reviewer wrote a `probe` key into `~/.config/better-ccflare/better-ccflare.json` because one probe command was missing a variable assignment, so a `Config` was constructed with **no path** and fell through to `resolveConfigPath()`. It reversed it itself and the end state was verified independently. The cause is identical all three times: **a path that silently defaulted, not an agent that disobeyed.** The general property is that **a test seam whose "no argument" case resolves to production is unsafe whoever calls it**, so the remedy is a constructor that throws under a test environment variable (`SB23-2277`), not a stricter brief
- **`/v1/*` and `/messages/*` are proxy passthroughs handled in `apps/server/src/server.ts` and belong in NO `api-catalog.ts` entry.** Measured, not reasoned: adding a probe entry `GET /v1/models/:model` **fails** the guard test on "every dynamic catalog route falls under a router prefix branch", so the guard does reject a stray `/v1/` entry and there is no gap to file. A sentence saying so is now in the catalog file's own header. Separately, `/v1/models` is **fed from** the passthrough at `packages/proxy/src/response-handler.ts:559-564` rather than served from the catalog, which is the opposite of what an issue asserted
- **`JSON.parse("null")` is a successful parse, so a try/catch never fires and optional chaining does not protect the base access.** A literal `null` body threw out of `processResponse`, the throw landed in `proxy-operations.ts`'s catch, which reads it as an account failure, so **one intermediary answering `null` would walk the entire account pool and report every account as failed.** Pre-existing on the listing path, fixed on both in `#143`
- **The OpenObserve exporter retries as of `#144`, and eviction under retry is strictly worse than before, deliberately.** A failed retryable batch goes to the **front** of the same bounded buffer, so it is evicted **before** newer records; the alternative protects a batch because it failed, which is a priority inversion that drops fresh records to keep stale ones. Retryable means a `fetch` throw, 429 and 5xx only, so a 401 storm cannot flood. `MAX_BATCH_ATTEMPTS = 6` then drops and counts, because **bounded loss is this exporter's advertised contract and an unbounded stall is not**: retry-to-front had created head-of-line blocking where one oversized batch answered with a 5xx stopped the whole stream until a thousand newer records evicted it. `MAX_BATCH_ATTEMPTS` itself is reasoned, not measured
- **Mutation hygiene, three failures from one night.** A mutation that **did not apply** looks exactly like one that survived, and the wrong conclusion sends you to strengthen a test that was already fine: the tell is `git checkout` printing `Updated 0 paths from the index` instead of `Updated 1 path`, so assert the anchor count **before** writing. A bounds test where the code removes before it puts back is measuring returned room rather than the bound (`mem:borrowed-room-cannot-overflow`). And a mutation harness pointed at a path with **uncommitted** work in it will eat that work on revert; it should refuse rather than the operator remember
- **Two CLI Performance tests assert wall-clock under 1s and fail on unmodified `main` under load**, measured at 1045ms and 1117ms with sixteen concurrent agent sessions. So **a lane reporting 2 failures and a lane reporting 4 can both be honest at the same head**, and a differing failure count indicts the instrument before the code. `SB23-2266`; the recommendation is to delete the assertion rather than raise it
- **Account `ICLD` has been failing OAuth refresh with `invalid_grant` at least hourly since 2026-09-17 05:21**, which is most of the 25 unacknowledged Insights alerts. `SB23-2262`, `ready-for-human`: `bun run cli --reauthenticate ICLD`
- **The `WorktreeCreate` isolation hook cannot create a worktree in this repo.** It runs `git worktree add -b <name> -- <path> main`, and this repo has both a `main` branch and a `main` tag, so it dies on `fatal: ambiguous object name: 'main'`. Any lane trying `isolation: worktree` hits it; the fix is in the hook, not the lane

- **Six more PRs merged 2026-09-19 from four parallel lanes**, `main` at `04953009`: `#151` and `#155` the account card's three rows and overflow menu (`SB23-2254`), `#152` alert grouping (`SB23-2239`), `#153` `GET /api/analytics/models` (`SB23-2260`), `#154` and `#156` the Codex credit balance (`SB23-2257`). Every lane again overruled the orchestrator at least once and was right
- **A worktree never receives its dispatch brief, because `.claude/` is gitignored.** `.gitignore:5` ignores `.claude/` and `git worktree add` populates only **tracked** files, so a brief written into the main checkout's `.claude/dispatch/` does not exist at the path the kickoff prompt names. All four lanes of the 22:52Z batch were dispatched with no brief. **Copy the brief into the worktree after creating it and verify it is non-empty before spawning.** The failure is silent, the peer improvises, and it is worse than silent: `BTCF-1760` **said** "I had no brief file to read, so I set 90 from my own estimate rather than yours", and the orchestrator read that as a discipline problem and corrected the peer. A peer reported the exact cause and it was attributed to the peer
- **Attribution can never happen on a repository's first request, and that is correct behaviour.** Discovery creates that repository's `projects` row **from** that request and the row lands **after** the request is written, so the resolver correctly resolves nothing. Measured against `AirPrint_Bridge`: request at 22:56:56, `projects.discovered_at` at 22:56:59, attempts one and two null, attempt three `83e7a6b7b4c40558` and joining. **A slug with a null `project_id` therefore has at least two causes with identical row shapes**: a working first request, and a header nothing parses. Never verify attribution with one request against a fresh path, because it fails against correct code. See `mem:first-request-cannot-be-attributed`
- **`SB23-2268`'s 436-row evidence was not clean, and its central claim was falsified by the lane implementing it.** The final count is **19** files naming `X-CCFlare-Path` with a leading `~`, **zero** with an absolute path, and **4** naming `X-CCFlare-CWD` with an absolute path (23 files under `~/Code`, read 23:05Z). A tilde in a JSON string reaches the resolver as literal text and prefix-matches no stored `canonical_path`, all of which are absolute and lowercased, so **the alias merged in `#149` attributes none of the existing files.** It is a header-contract correctness fix, and `SB23-2268` closing does **not** mean attribution is fixed. What repairs those repos is `claude-meta`'s SessionStart hook writing `X-CCFlare-CWD` with a real absolute cwd, which is why that count is 4 rather than 1: the drift between the counts is the hook working, not two instruments disagreeing. `SB23-2283` asks whether a tilde survives Claude Code into the sent header at all. Also: `.claude/settings.json` is gitignored here, so an acceptance line asking a PR to correct this repo's own headers **cannot be satisfied by a commit**
- **An expanded issue's counts are a starting hypothesis, not evidence, and three issues had their own proposals falsified in one evening.** `SB23-2055` declared the PostgreSQL migration path untested when a harness exists; `SB23-2260`'s proposed query-parameter arrays were wrong in **both** directions, keeping `bucket`, which is derived inside `getRangeConfig` and read from no query string, and omitting `apiKeys` and `status`, which `buildRequestFilters` does read; `SB23-2268`'s central claim above. The reason is structural rather than sloppiness: `SB23-2268`'s evidence lived in the **host's filesystem**, not in the diff or the database, so only the implementing lane could see it. Expansion is worth its cost and its output still needs checking at the point of use
- **Five `api-catalog.ts` entries named query parameters no handler reads and omitted ones every handler does, and the drift guard cannot see this class.** `/api/analytics` listed `bucket`, `model`, `accountId`, `projectId`; `/api/usage-history` listed `accountId` where the handler requires `account` and **400s** without it; all three `/api/insights/*` listed `range` alone while calling `buildRequestFilters`. So a caller building a request from `GET /api/meta/routes` got a silently ignored filter or a 400. Fixed in `#150`. The guard compares paths and methods and **reads no query names**
- **An apostrophe in a SQL comment silently breaks placeholder rewriting on PostgreSQL, and SQLite cannot see it.** `convertPlaceholders` in `BunSqlAdapter` finds string literals by counting apostrophes across the raw statement and skips neither `--` comments nor apostrophes inside them. A comment reading `-- ... so AVG's NULL-skipping` left the scanner believing a literal was open, so the `?` in the following WHERE clause was never rewritten to `$1`: four of five live cases returned HTTP 500 with `PostgresError: syntax error at or near "AND"`. **23 tests and 4 mutations were green with the defect present**, because SQLite never receives the rewritten form, and **the error names `AND`**, which is neither the apostrophe nor the `?`. The existing static gate reads `--` comments for `?` and not for `'`, which is the same hazard through another door. `SB23-2286` carries the class, its gate, and three pre-existing offenders at `migrations-pg.ts:580`, `:641` and `request.repository.ts:227`
- **A deleted remote branch plus a stale local branch equals a silent resurrection, and two lanes hit it in one night.** A merge with `--delete-branch` removes the remote branch; a lane that then pushes without fetching **recreates** it from history that predates the orchestrator's `update-branch` merge. **The only warning is `git push` printing `* [new branch]` where you expect `a..b`**, and the secondary tell is `git fetch` reporting `couldn't find remote ref` or a merge simulation dropping from five files to one. Neither conflicts, neither errors. Both lanes reversed it themselves and verified with `git ls-remote`
- **`tail -n 500` bounds lines, not bytes.** A log trim using it left a 300,413-byte single-line file untouched and reported success. That was the **fourth** guard in one evening that reported success having done nothing, beside a `chmod` returning 0 without changing a mode, a bounds test that could not overflow, and a mutation whose anchor never matched
- **A suite line can be arithmetic rather than a measurement, and the one check meant to catch a fabricated line passes on it.** A lane wrote `4884 pass / 4942 total` into a PR comment before the run finished, deriving all four numbers from the previous head plus the tests it had added, against a real `4885 / 4943`. Deriving every number **preserves the pass + skip + fail sum by construction**, so the sum check cannot detect it. The same lane also quoted a head sha to its reviewer while the commit was still running, and that sha is not a commit in this repository. Both corrected in public rather than edited away. A value that normally comes from a command, produced by inference at the moment it is needed, reads exactly like the measured values around it
- **A sub-agent that looks idle and has cleaned up may still be holding its findings.** `SB23-2260`'s reviewer wrote three full reports as assistant text without ever calling `SendMessage`, so they sat in its context for twenty minutes while its worktree was already gone and it appeared to have finished without reporting. A second reviewer was spawned on that assumption; both verdicts were real and agreed. Separately, a lane removed its reviewer's worktree while the reviewer was still running, having read its verdict file and taken that as the report, and the reviewer lost the file it was mid-way through writing. **Never tear down a peer before it reports, and never read a peer's artifact as its report**

- **v3.23.0 is the current release, tag at `348dda83`, 8 assets across 5 targets, tap formula confirmed pointing at the v3.23.0 tarballs.** It carries twenty-two PRs. **`v3.23.0-rc.1` is a dead tag and stays**: its `Source quality` job failed on five OAuth handler cases answering HTTP 500 at a commit whose CI was green, because `oauth.ts:878` and `:971` construct `new Config()` with no path and `#159`'s guard refuses that under `NODE_ENV=test` unless a redirect is set. The file had only ever passed because **another test file in the same process set `XDG_CONFIG_HOME` first**, so the result depended on file order and CI and the release profile ordered them differently. `#171` gives that file its own redirect; with all three redirect variables unset the full suite then reads 5010 pass / 65 skip / 2 fail of 5077, which is the measurement saying no other file carries it
- **A `Publish Homebrew Formula` failure is the tap, and it happened again.** v3.23.0 published its GitHub release and all 8 assets, then failed at `brew test-bot --only-tap-syntax`, which lints the **whole tap**: `Formula/yabai.rb:8` tripped `Homebrew/OSDependsOn` on a redundant `depends_on macos: :big_sur`. One offence in any formula fails publication for every project in the tap. Fixed in the tap at `f18c79b` and republished with `gh workflow run hextap-release.yml --repo SijanC147/better-ccflare --ref main -f tag=v3.23.0`. Identical in shape to the `headphones.rb` `def post_install` failure that blocked v3.10.0
- **A change in the config-permission family earns an independent reviewer whatever the line count says.** The mechanical threshold is roughly 60 non-test non-comment added lines and it stays mechanical everywhere else. This family has now produced reachable defects at **16** lines (`#169`, a hardlink carrying the inode's owner to a new name, so a uid of ours does not mean we created the entry; the planted link made `config.get("local_control_secret")` return an unrelated file's contents and `restrictConfigFile()` then chmodded that file through the link) and at **21** lines (`#172`, a refusal skipping `restrictConfigDir()` so a hardlinked config left the directory at 0755, and a directory at the config path diagnosed as a hardlink). `#57` in the same family took fourteen review rounds. Two measured false negatives in a row is not an argument for moving the threshold; it is an argument for one named exception
- **From inside a worktree, name `origin/main`. Never `refs/heads/main`.** `main` is checked out in the main checkout, so no linked worktree can fast-forward its own `refs/heads/main`, and it is stale from the moment the worktree is created. A lane measuring its own diff against the stale ref read **39 logic lines across four files** where the truth against `origin/main` was **21 across three**, which is the difference between earning a reviewer and not. This applies to the reviewer-threshold command and to every `git merge-tree` merge simulation
- **`chmodSync` silently drops the sticky bit on Linux, and it is Bun rather than the kernel.** Measured in a container: `chmodSync(dir, 0o1777)` yields `1777` on macOS and **`0777`** on Linux, for an octal literal, the same value in decimal, and the string `"1777"`, running as root, while coreutils `chmod 1777` in the same container gives `drwxrwxrwt`. Reading is unaffected: `statSync("/tmp")` reports 1777 on Linux. So a fixture that manufactures a sticky directory works on macOS and silently does not on CI, which is how three tests failed on Linux while passing locally. `SB23-2319`. The fix that worked was not a platform gate but removing the attempt to manufacture the property: try the chmod, check whether it took, otherwise use `os.tmpdir()` itself, which on Linux already is `/tmp` at 1777. **That fixture also carried a latent `rm -rf /tmp`**, because on the Linux route the fixture directory *is* `os.tmpdir()` and the tests removed it recursively in a finally block
- **Three separate causes produced the three pre-existing PostgreSQL live-harness failures, and one was a real product defect.** `getRequestsByAccount` (`request.repository.ts:652`) selected `a.name` while grouping by `r.account_used` alone: PostgreSQL rejects with 42803, SQLite tolerates a bare non-aggregated column. It was never a live 500 because no HTTP handler calls it. The other two were fixture defects, one of them `Object.keys()` on a result array, which holds on SQLite and reads 7 for 3 rows on PostgreSQL because **Bun's SQL driver hangs four extra enumerable own properties off result arrays** (`count`, `command`, `lastInsertRowid`, `affectedRows`). `#168` normalises that with `Array.from` in the adapter's PG arm. **The live-harness baseline is now 65 pass / 0 fail / 0 skip**; the old "same three failures by name" check is retired, so any live-PG failure is a regression
- **No test file in this repo is typechecked.** The root `tsconfig.json` sets `include` to `packages/*/src/**/*` and `apps/*/src/**/*` and then excludes `**/__tests__`, `**/*.test.ts` and `**/*.test.tsx`, so a blatant `const x: number = "string"` in a test file produces **zero** diagnostics. `#162` closed it for `packages/dashboard-web` only, by way of that package's own tsconfig, and fixed the eight errors that surfaced. Repo-wide the exclusion hides **1374** errors, with providers, proxy and http-api holding 1252 of them. `SB23-2304`, estimate 5, not started

- **Run `bunx biome check` only after `bun install`, or it measures a different tool.** A lane ran it in a fresh worktree before installing, `bunx` resolved an unpinned biome, the two files passed, and CI then failed them. It fails in the direction that matters: the unpinned tool says **clean**. The same applies to any `bunx` gate in a worktree that has not installed yet, and a fresh worktree needs `bun install` anyway, because `typecheck` dies on a `bun-types` error without it
- **The suite-line sum check cannot see a shrunken program, and it happened twice more.** One run read `5054 pass / 65 skip / 6 fail of 5125` where the head's real total is **5130** at the same 402 files, and `5054 + 65 + 6 = 5125` sums perfectly, so the one check meant to catch a fabricated or corrupted line **passes on the corrupted one**. The tell is the **total against a measured baseline**, not the internal sum and not the failure count: a concurrent `bun test` in the same worktree, often the lane's own reviewer, removes tests from the program entirely. `pgrep -fl "bun test"` before starting, and compare the total and the file count against a baseline run at the merge base
- **Keying a guard on an environment variable does not set it, so a test asserting the variable is unset passes whatever the guard reads.** A lane disclosed two such assertions as the line it was least sure of; its reviewer mutated the guard to key on exactly the variable they named and **both assertions still passed**. They killed nothing and could only ever produce a false red. Removed. To pin which marker a guard uses, set each candidate in turn and assert the behaviour changes, never assert the environment

- **A sub-agent's claim that it created or changed something is checked by reading the thing, never by believing the message.** One reviewer wrote **four** verdicts that the transport delivered where neither the lane nor the orchestrator reads, surfacing to the lane only on its final turn, so it reported "the reviewer went silent" at the moment its approval sat undelivered. **"It never reported" and "it reported four times into an unread channel" are indistinguishable from outside and demand opposite responses.** Five sub-agents have now finished, torn down their worktrees and gone idle holding output. The rules that follow from it: a verdict is not delivered until the sub-agent has called `SendMessage`, and an idle notification is not a delivery; say so in the brief when spawning one; before reporting "no verdict", call `ListAgents` and write **"idle, no verdict received"** rather than "silent", which carries the doubt; and check a filed issue id with `get_issue`, a pushed commit with `git ls-remote`, a merged file with a blob hash. One action covers both the delivery failure and the fabrication case, and it does not depend on the transport behaving
- **A label asserting what a command proved, written before its output was read, is a fabricated measurement even when the claim turns out true.** A lane verified a cherry-pick with `git diff <a> <b> --stat` and printed "(empty diff above = content identical)" underneath it. The diff was **not** empty, it listed 9 files, because the two commits sat on different bases and it was showing every other lane's merges. The label would have read as a passing check to anyone skimming, including its author. The decisive check for identical content across different bases is the **blob**: `git rev-parse <commit>:<path>` on both. Write the label after reading the output, or do not write one
- **A ledger of assertions needs a rule for what counts as new before anyone writes a number.** A lane corrected its `as`-cast ledger twice and it was wrong both times, because a re-spelling of an existing assertion at an existing site is not a net-new assertion and nothing had said so in advance. Without the rule stated first, the number is unfalsifiable and every correction produces another one. State the rule, then count

- **v3.24.0 is the current release, tag at `092caa0d`, 8 assets across 5 targets, tap formula confirmed pointing at the v3.24.0 tarballs.** RC-first on the same commit as always, and the Homebrew publication succeeded without a tap repair this time. It carries **25 PRs**, and `main` is past it again by eight. The substance is two series: the **config-trust hardening** (`#169` sticky-directory trust and the hardlink that carries an inode's owner to a new name, `#172` a regular file at the config path never trust-checked, `#176` the file's own mode never consulted, `#182` and `#184` the cross-uid bind mount, `#186` the unclearable ERROR, `#190` diagnosis-once-outcome-every-time, `#194` the unexaminable-directory catch, `#200` the chmod seam) and the **typecheck gates** (`#162`, `#180`, `#188`, `#193`, `#196`, `#199`, `#201`), which took eleven packages from unchecked test files to gated and cleared roughly 590 type errors. **v3.23.0's tag `348dda83` and v3.24.0's `092caa0d` both predate nothing the host runs**: Sean is still on 3.22.0, so the `#140` backup file-mode fix has now been shippable across two releases and has still never executed
- **Read `docs/worktree-lane-brief.md` before dispatching a worktree peer.** It is the 36 standing rules appended to every lane brief on this project, tracked rather than left in `.claude/`, because `.claude/` is gitignored and `git worktree add` populates only tracked files, so a brief written into the main checkout does not exist at the path the lane's kickoff prompt names. Each line is a measurement with its PR number, not a preference. The task-specific half of a brief still has to be **copied into the worktree and verified non-empty before spawning**
- **"Approve with findings" is a state, not a verdict, and merging on it shipped two unaddressed must-fix items.** `#182` merged while its lane was mid-edit applying them, so a deployment doc quoted the refusal message **that same commit had replaced**, which is the one thing a sample exists for. The lane's own fix is the rule: report **"ready, no findings"** or **"ready pending a fix round"**, never just "reviewer approved", because from outside the two are indistinguishable. A verdict covering an earlier head is a different and acceptable thing when the later commits are the fixes it asked for. See `mem:approve-with-findings-is-not-ready`

- **`gh pr merge --delete-branch` removes the lane's worktree, and it did it here while a lane was mid-report.** Git requires the worktree gone before it can delete the branch; gh's help text mentions only branches. On 2026-09-21 `gh pr merge 209 --merge --delete-branch` destroyed BTCF-2343's working directory in the same minute the orchestrator asked it for its close-out, and its first verification command died on "Working directory was deleted". It finished only by reading the content out of the main checkout with `git -C <main> show <sha>:<path>`. **Never pass the flag**; delete the branch as a separate teardown step after the peer reports. It fires on `--merge`, not only `--squash`, and it fails unevenly: when untracked files remain the removal fails, the branch is not deleted, and a `.git`-less directory is left behind, which is the state `2387/` was found in. Full detail in `mem:global/worktree-peer-teardown-hazards`, where the rule had been recorded since 2026-09-14 and did not bind
- **A count of type errors undercounts the defect population by exactly the casts that silence it.** `meta/__tests__/provider.test.ts` carried the identical missing-field defect as SB23-2441's seventeen files and reported **zero**, because its literal ended in `as Account`; it was in nobody's slice precisely because the cast hid it. Measured on the three free-and-clear files: **9 errors hidden across 3 files**, 9 of the original 87 that defined all five slices. Package-wide the ten test-literal sites hide **12**. So before splitting a package by error count, grep for `as <Type>` and `@ts-expect-error` over the same paths and state that number beside the error count (`SB23-2449`). **A partial mask is not a full one**: `alibaba.test.ts`'s cast is *already failing* with `TS2352`, so removing it swaps one error for a `TS2353` naming `status`, a field on no version of `Account`, and a lane counting "cast present = invisible" gets that file wrong. The same shape at runtime is `SB23-2457`, where a cast conjures `_vertexModel` onto a shared `Account` and hides a state-lifetime question no tool can see
- **A mutation against a shared fixture measures the tests that consume that fixture, not the tests that look like they do.** Mutating `createMockErrorResponse` to `ok: true` in `oauth.test.ts` killed **3 of 4**; the fourth built its mock inline at `:282` and needed its own mutation. **3 of 4 reads as a strong result while the survivor reads as a weak test**, and nothing in the output separates "tolerates the defect" from "never saw it". Count the tests that actually consume the fixture before believing a fixture-level mutation. Fixture-level mirror of `mem:covering-the-function-is-not-covering-the-call`
- **A matching file count does not detect a shrunken test program, and `pgrep` before a run does not either.** Two runs at one head on 2026-09-21 read **5149** and **5134**, 15 apart, at the same 403 files, with the internal pass + skip + fail sum passing on both. The contention was the lane's **own reviewer sub-agent** running focused suites in its worktree, which is the source nobody checks. **A lane must not run its full suite while its own reviewer is running one.** **`pgrep` is a hint; the total against a measured baseline is the check.** On 2026-09-21 a lane's third run read `pgrep` clean before and **3 after**, so a competitor did start during it, and the run was still sound because the total matched: a competitor only shrinks the program if it removes files from *this* run's program, which the total is what detects. Report one run's line rather than averaging or picking the better one, and say which runs were discarded. An earlier lane's claim that the 402 file count was the check was retracted by its own author: the recorded incident is 5125 against a real 5130 at the same 402 files
- **A baseline can measure the wrong tree and look perfect.** A lane reconstructing main's program with `git checkout origin/main -- $F`, three paths space-joined in one variable, got `pathspec ... did not match any file(s)` on both calls and **silently measured its own head while labelling it the baseline**, reading an exact match. The suite exits 1 either way on the two known macOS failures, so only reading the output caught it. Same shape as the standing "never pass a space-joined variable to `bun test`" rule, one layer up in git
- **Script a mechanical multi-file edit rather than hand-editing it, and count what is left.** Two independent derivations on 2026-09-21. From the failure side: BSD `sed` rejects `\|` alternation in a basic-regex group, so a 12-site edit applied **6** and exited 0, caught only by grepping the remaining anchors. From the recovery side: a lane lost an uncommitted fix to `git checkout --` while reverting a probe, and the cost was **zero** because re-running its brace-matched script reproduced the file byte-identically by error code. Also from that batch: a mechanical wrap turned seven local helpers into `function makeAccount(...) { return makeAccount({...}) }`, which **typechecks and is infinite recursion**, caught by counting inserted call sites
- **`?.` in a test is a silent skip and is only as good as the assertion above it.** `expect(x).not.toBeNull()` is a runtime assertion that narrows **nothing** for TypeScript, so `x?.()` on the next line invokes nothing if the guard is ever moved, and the test still passes. Third instance here: `cache-body-store.test.ts`'s four surviving `?.` (`SB23-2448`), `codex/provider.test.ts:5247-5251`, and the `TS2349`/`never` pair whose `never` is a **control-flow artifact** rather than a missing annotation, because the variable is assigned only inside a stored callback. **The conversion that satisfies the compiler is exactly the one that removes the runtime failure**, so read each site's guard rather than converting mechanically
- **`expect.fail` does not exist in Bun and the guards using it survive by accident.** `typeof expect.fail` is `undefined` on 1.4.2; calling it throws a `TypeError` which the guard's own `catch` swallows, and the next assertion (`toBeInstanceOf(OAuthError)`) then rejects it. So coverage held and only the **diagnostic** was wrong. A `catch` asserting a loose `error.message` regex, or nothing, would have been genuinely silent. Cleared repo-wide: one mention remains, a docstring at `oauth.test.ts:70`, verified with `grep -rnF`
- **The gate belongs to whichever lane observes zero, never to a name chosen in advance.** SB23-2445 was flagged as the likely carrier of `typecheck:providers` on the assumption the five slices would land in sequence. They ran in parallel, so a slice landing second would have gated the package with four slices of errors outstanding and turned main red. Both halves are needed when it does land: `"exclude": ["node_modules", "dist"]` in `packages/providers/tsconfig.json` **and** the chain entry in the root `package.json`, because without the chain entry the gate never runs
- **The host is on v3.24.0 as of 2026-09-21 and the `#140` backup sweep has finally executed.** pid 1179 resolves to `/opt/homebrew/Cellar/better-ccflare/3.24.0/bin/better-ccflare`, and all six files under `~/.config/better-ccflare/` matching `better-ccflare.db.backup.*` now read **0600**. That closes the exposure that stood through two releases: the fix merged in `#140`, shipped in v3.23.0 and v3.24.0, and did nothing until the host ran the binary carrying it. The general rule stands and is now closed rather than merely stated: **a merged fix is not a deployed fix**

## Commit Messages

Conventional-commit prefixes (`feat:`, `fix:`, `security:`, `refactor:`, …). Convention only — nothing parses them. The old prefix-driven release automation was removed with `release.yml`; the git tag alone determines a release.