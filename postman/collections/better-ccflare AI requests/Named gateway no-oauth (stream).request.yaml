$kind: llm-request
description: |-
  Streamed through the `no-oauth` gateway. Needs a gateway named `no-oauth`. Create it once with the `PUT /api/openai-gateways/:name` request in the better-ccflare API collection (name `no-oauth`, body `{"exclude_providers": ["anthropic-oauth"]}`), or change the name in the URL to one from `GET /api/openai-gateways`.
url: 'http://localhost:8080/v1/gateways/no-oauth'
config:
  model: 'claude-opus-5-5'
  provider: openai-compatible
settings:
  streamResponse: true
  maxToken: 256
userPrompts:
  - id: c3433d26-ce60-427d-ced0-59aeba15000b
    value: 'Reply with the single word: pong'
    timestamp: 1790208000000
    active: true
    contentType: text
mcpConfig: ""
order: 4000
