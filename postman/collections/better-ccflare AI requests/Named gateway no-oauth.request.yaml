$kind: llm-request
description: |-
  The `no-oauth` gateway excludes Claude OAuth accounts. Needs a gateway named `no-oauth`. Create it once with the `PUT /api/openai-gateways/:name` request in the better-ccflare API collection (name `no-oauth`, body `{"exclude_providers": ["anthropic-oauth"]}`), or change the name in the URL to one from `GET /api/openai-gateways`.
url: 'http://localhost:8080/v1/gateways/no-oauth'
config:
  model: 'claude-opus-5-5'
  provider: openai-compatible
settings:
  streamResponse: false
  maxToken: 256
userPrompts:
  - id: 560d23f7-68fd-f5c2-01d8-29cfdbdc2ca0
    value: 'Reply with the single word: pong'
    timestamp: 1790208000000
    active: true
    contentType: text
mcpConfig: ""
order: 3000
