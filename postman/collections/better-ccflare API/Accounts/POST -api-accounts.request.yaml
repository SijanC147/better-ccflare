$kind: http-request
name: 'POST /api/accounts'
description: |-
  Add an account (generic entry point).
  
  Body: Provider-specific. See the per-provider routes below.
method: POST
url: '{{baseUrl}}/api/accounts'
order: 2000
headers:
  - key: 'Content-Type'
    value: 'application/json'
body:
  type: json
  content: |-
    {}
