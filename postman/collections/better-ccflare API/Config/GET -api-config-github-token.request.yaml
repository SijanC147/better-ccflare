$kind: http-request
name: 'GET /api/config/github-token'
description: |-
  Whether a GitHub token is stored. The token is never returned.
method: GET
url: '{{baseUrl}}/api/config/github-token'
order: 32000
