$kind: http-request
name: 'POST /api/config/github-token'
description: |-
  Store a GitHub token.
  
  Body: Carries a credential. The playground never logs a request body — see the PR body.
method: POST
url: '{{baseUrl}}/api/config/github-token'
order: 33000
headers:
  - key: 'Content-Type'
    value: 'application/json'
body:
  type: json
  content: |-
    {}
