$kind: http-request
name: 'POST /api/config/retry'
description: |-
  Set retry_attempts, retry_delay_ms and retry_backoff. Out-of-range values are rejected.
method: POST
url: '{{baseUrl}}/api/config/retry'
order: 38000
headers:
  - key: 'Content-Type'
    value: 'application/json'
body:
  type: json
  content: |-
    {}
